# ============================================
# Ansible MCP Server - Environment Configuration
# ============================================
# Copy this file to .env and customize values
# NEVER commit .env to version control!
# ============================================

# ===== REQUIRED CREDENTIALS =====
# Generate with: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET

# ===== DATABASE =====
POSTGRES_USER=ansible_mcp
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
POSTGRES_DB=awx
WEB_DB_NAME=ansible_mcp

# ===== SECURITY =====
VAULT_ROOT_TOKEN=REPLACE_WITH_VAULT_TOKEN
AWX_SECRET_KEY=REPLACE_WITH_AWX_SECRET
GITLAB_ROOT_PASSWORD=REPLACE_WITH_GITLAB_PASSWORD
GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_GRAFANA_PASSWORD

# ===== AI PROVIDER =====
# Options: openai, anthropic, gemini, ollama
AI_PROVIDER=openai
AI_MODEL=gpt-4

# OpenAI (if AI_PROVIDER=openai)
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic (if AI_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google Gemini (if AI_PROVIDER=gemini)
# GEMINI_API_KEY=your-gemini-api-key

# Ollama (if AI_PROVIDER=ollama)
# AI_BASE_URL=http://localhost:11434

# ===== OPTIONAL SETTINGS =====
# Logging: debug, info, warn, error
LOG_LEVEL=info

# MCP Authentication (enable for production)
# MCP_API_KEY=your-secure-api-key
# MCP_ENABLE_AUTH=true

# Redis (if using external Redis)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# Metrics port
# METRICS_PORT=9090
